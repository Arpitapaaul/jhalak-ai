export default function CandidateCard({ candidate }) { return <article className="candidate-card"><img src={candidate.image} alt={candidate.title}/><h3>#{candidate.rank} · {candidate.title}</h3><p>{candidate.source}</p><footer><span className={candidate.match?'match-label':'no-match-label'}>{candidate.match?'● MATCH':'○ BELOW THRESHOLD'}</span><span>{candidate.score}</span></footer></article> }
